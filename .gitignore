# Neovim swap, backup, and undo files
*.swp
*.swo
*~
.netrwhist
# Files generated by Neovim's persistent undo feature
*.un~

# Package manager lockfiles/downloads (if you want to reinstall them freshly per machine)
# Uncomment the line below if using lazy.nvim and you don't want to track installed plugins
doc/tags
lazy-lock.json

# LSP and parser caches 
.luaversion
.lazy.json

# Python/Node host virtual environments (if local to your config)
.venv/
node_modules/
